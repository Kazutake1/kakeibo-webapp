name: iPhone income verified release
on:
  push:
    paths:
      - '.github/workflows/patch-income-mobile-v2648.yml'
permissions:
  contents: write
jobs:
  verify-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
